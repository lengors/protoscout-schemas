# yaml-language-server: $schema=https://json-schema.org/draft-07/schema
$schema: http://json-schema.org/draft-07/schema
$ref: "#/components/schemas/scraper-specification-request-parser"
$id: io/github/lengors/protoscout/domain/scrapers/specifications/models/000-scraper-specification-request-parser.yml

components:
  schemas:
    scraper-specification-request-parser:
      javaType: io.github.lengors.protoscout.domain.scrapers.specifications.models.ScraperSpecificationRequestParser

      type: string
      title: Scraper's request response parser.
      description: Parser to use to parse request's response.
      enum:
        - html
        - json
        - text
