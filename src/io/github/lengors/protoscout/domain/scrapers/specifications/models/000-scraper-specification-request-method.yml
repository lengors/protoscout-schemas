# yaml-language-server: $schema=https://json-schema.org/draft-07/schema
$schema: http://json-schema.org/draft-07/schema
$ref: "#/components/schemas/scraper-specification-request-method"
$id: io/github/lengors/protoscout/domain/scrapers/specifications/models/000-scraper-specification-request-method.yml

components:
  schemas:
    scraper-specification-request-method:
      javaType: io.github.lengors.protoscout.domain.scrapers.specifications.models.ScraperSpecificationRequestMethod

      type: string
      title: Scraper's request HTTP method.
      description: HTTP method used for scraper's request.
      enum:
        - get
        - head
        - post
        - put
        - delete
        - patch
