# yaml-language-server: $schema=https://json-schema.org/draft-07/schema
$schema: http://json-schema.org/draft-07/schema
$ref: "#/components/schemas/scraper-response-result-date-time-instant-grain"
$id: io/github/lengors/protoscout/domain/scrapers/models/scraper-response-result-date-time-instant-grain.yml

components:
  schemas:
    scraper-response-result-date-time-instant-grain:
      javaType: io.github.lengors.protoscout.domain.scrapers.models.ScraperResponseResultDateTimeInstantGrain

      type: string
      title: Result date-time instant granularity.
      description: Granularity of date-time instant associated with scrapped result.
      enum:
        - second
        - minute
        - hour
        - day
        - week
        - month
        - quarter
        - year
